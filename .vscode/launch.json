{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch TS",
            "type": "node",
            "request": "launch",
          
            "cwd": "${workspaceRoot}",
            "program": "${file}"
        },
    ]
}